# ``Heap_Primitives``

@Metadata {
    @DisplayName("Heap Primitives")
    @TitleHeading("Swift Primitives")
}

A binary heap / priority queue with configurable min/max ordering — O(log n) insert and remove, O(1) peek — over move-only or copyable elements.

## Topics
